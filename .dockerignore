# Version control, local state and generated artifacts
.git
.gitignore
.env
*.db
*.db-shm
*.db-wal
coverage.out
/target/
/dist/
/data/

# The production image copies only the Cargo workspace. These trees are legacy
# sources and test tooling kept outside the image build context until their
# deletion stage is complete.
/web/
/internal/
/cmd/
/data-plane/
/go.mod
/go.sum

# Local test output and demo material
.demo-shots/
